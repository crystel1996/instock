import { FC } from "react";
import { ResendCodePageInterface } from "./interface";
import { ResendCode } from "../../Component";

export const ResendCodePage: FC<ResendCodePageInterface> = () => {
    return <>
        <ResendCode />
    </>
}